1. I want the series structure to have two tables, one for series and another for episodes. It seems currently there is only one table. This will allow the ignore property to live in the series table (one entry). And the title and link to the series will also live there.
2. Need to add the "Read" view to movies and news feeds. Currently only shows unread, can not see already read. And it should be possible to change read items into "unread" again.
3.1. There has to be 4 tabs in the Pelis feed: Filtered view (current view), Non-filtered view (what's non-read, but was filtered), Filtered and read, Non-Filtered and read.
3.2. Series should work in the same way. We now have filtered, All, Read. But there should be the same categories. Maybe it is a 2 Filtered, non-filtered. And within each you have "show non-read, read, or all".
More??
