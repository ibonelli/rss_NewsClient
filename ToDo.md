I want the series structure to change from the current single table, to two tables: One for series and another for episodes. This will allow the "ignore" property to live in the series table. The title and link to the series will also live in the first table. The second table will have the episodes, formats & other episode related information.

I want to first work on the documentation we have (use the sdlc skill if you need to). Make all the questions you need so we have a proper documentation. Only once you wrap that up and save the documentation changes, start working on the code changes.

1. Need to add the "Read" view to movies and news feeds. Currently only shows unread, can not see already read. And it should be possible to change read items into "unread" again.
2.1. There has to be 4 tabs in the Pelis feed: Filtered view (current view), Non-filtered view (what's non-read, but was filtered), Filtered and read, Non-Filtered and read.
2.2. Series should work in the same way. We now have filtered, All, Read. But there should be the same categories. Maybe it is a 2 Filtered, non-filtered. And within each you have "show non-read, read, or all".
More??
